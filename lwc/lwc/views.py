from django.shortcuts import render # render templates for us

"""
def home(request): # handles home page
	context = {}  # context will equal python dictionary
	template = "home.html"
	return render(request, template, context)
	
def home2(request): # handles home page
	context = {}  # context will equal python dictionary
	template = "home2.html"
	return render(request, template, context)
"""